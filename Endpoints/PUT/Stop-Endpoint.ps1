<#
    .DESCRIPTION
        This script will Stop a single Endpoint
    .EXAMPLE
        Stop-Endpoint.ps1 -RequestArgs $RequestArgs -Body $Body
    .NOTES
    	This will return data
#>

param(
    $RequestArgs,
    $Body
)

# Add Code to Stop an endpoint
# Request Args should include a path to a service definition file