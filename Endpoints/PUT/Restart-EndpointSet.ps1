<#
    .DESCRIPTION
        This script will Restart a group of Endpoints
    .EXAMPLE
        Restart-EndpointSet.ps1 -RequestArgs $RequestArgs -Body $Body
    .NOTES
    	This will return data
#>

param(
    $RequestArgs,
    $Body
)

# Add Code to Restart a group of endpoints
# The request args should contain a directory where the Service definitions exist.